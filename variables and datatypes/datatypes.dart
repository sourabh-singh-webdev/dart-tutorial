void main() {
  // Every datatype in DArt is an object
  // And undeclared variable in dart are initialised with null

  /// Datatypes in dart are as following:
  /// 1).Number:---i). int - for number
  ///             ii). double - for decimals number
  ///
  /// Also, we can declare hexagonal number

  int age = 3;
  double marks = 12.5;
  print(age);
  print(marks);

  /// 2).Strings:--- Using the string keyword
  ///

  String name = 'sourabh singh';
  print(name);

  /// 3).Boolean:--- true or false
  ///

  bool isHappy = false;
  print(isHappy);

  /// 4).Lists:-- Similar to the array, or we can say collection of objects
  ///

  
}
